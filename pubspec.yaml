name: seniorcareapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  http: ^1.6.0
  provider: ^6.1.5+1
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.5
  intl: ^0.19.0

  # Versiones compatibles para notificaciones
  flutter_local_notifications: ^17.1.2
  timezone: ^0.9.2

flutter:
  uses-material-design: true